# Contributing

1. `npm install && npm run build`
2. `npm run test:unit && npm run test:integration`
3. Load `extension/dist` unpacked for manual checks ([docs/manual-smoke.md](docs/manual-smoke.md))
4. Keep Manual mode as default; never weaken prohibited-action hard blocks
5. PRs should update docs when changing permissions or protocol
